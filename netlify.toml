[build]
  command = """
    curl -fsSL https://bun.sh/install | bash &&
    export BUN_INSTALL="$HOME/.bun" &&
    export PATH="$BUN_INSTALL/bin:$PATH" &&
    bun install --production &&
    bun run build
  """

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/entry.mjs"
  status = 200
